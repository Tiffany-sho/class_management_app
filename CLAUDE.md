# R-Tech 管理システム

## アプリ概要

R-Tech の **プログラミング教室・イラスト教室（2事業）** の生徒・従業員を管理し、管理者・保護者・従業員が情報を共有するアプリ。
**Web（PC・スマホ）と iOS アプリの2形態**で提供する。

- ロールは3つ: **管理者 / 保護者 / 従業員（講師）**。生徒のアカウントは存在せず、保護者が代わりに操作する。
- 姉妹プロジェクト: シフト管理・給与計算アプリ（`../shift_manage_app`）。**同じ事業者**のアプリで、**デザインはそちらを基準に揃える**。あちらも同じ2事業を扱う。
- 規模: 生徒15名・従業員2名程度。

---

## ドキュメントの読み分け

**このファイルは目次。中身は各ドキュメントにある。作業を始める前に、下の表で該当するものを読むこと。**

| 作業 | 読むファイル |
|------|-------------|
| **画面レイアウト・色・余白・タイポグラフィ** | [`DESIGN.md`](DESIGN.md) — 唯一の情報源。**UI 作業では必ず読む** |
| **アプリの仕様・料金・守るべきルール**（「この画面は何をするのか」） | [`docs/domain.md`](docs/domain.md) |
| **SQL・テーブル・カラム・RLS・マイグレーション** | [`docs/data-model.md`](docs/data-model.md) |
| **技術選定・Web と iOS の関係・ナビゲーション構成・通知** | [`docs/architecture.md`](docs/architecture.md) |
| **「なぜこうなっているのか」「これは決まっているのか」** | [`docs/decisions.md`](docs/decisions.md) |

### どこに書くか

| 内容 | 書く場所 |
|------|---------|
| 事業のしくみ・機能ごとの仕様・崩してはいけないルール | `docs/domain.md` |
| テーブル・カラム・制約・RLS と、その設計を選んだ理由 | `docs/data-model.md` |
| 技術スタック・リポジトリ間の同期・画面構成・通知経路 | `docs/architecture.md` |
| **変わった決定・保留・未決定** | `docs/decisions.md` |
| 色・余白・コンポーネントのスタイル | `DESIGN.md`（shift_manage_app と同期） |

**迷ったら `docs/decisions.md`。** 判断が固まったら、そこから該当ドキュメントへ移す。

---

## 開発ルール

### 設定値は必ずマスタデータにする（最重要）

**開催曜日・コマ時間・コース・料金・定員係数といった運用値を、コードに定数として書かないこと。** すべてDBのマスタテーブルに持たせる。

- 理由: これらは事業側の都合で変わる。コードに埋めると、変更のたびに開発・デプロイが必要になる。
- **管理画面での編集が間に合わない場合でも、Supabase のダッシュボードから直接編集できる状態を必ず維持する。** そのため、マスタテーブルは人間が読んで意味の分かるカラム名・値にする（謎のコード値を使わない）。
- 判断基準: **「事業者が来年変えるかもしれない値」はすべてマスタ。** 迷ったらマスタにする。

### コンポーネント分割

- **なるべく細かくコンポーネントに分ける。** 1ファイル1コンポーネントを原則とする。
- 画面（ページ）コンポーネントは **データ取得と配置に徹する**。見た目は小さいコンポーネントに切り出す。
- **1ファイルが 200行を超えたら分割を検討し、300行を超えたら必ず分割する。**
- **Web と iOS で、コンポーネントの分割単位とファイル名を揃える。** 片方だけ細かく切ると、移植のときに対応が取れなくなる。
- 状態やロジックの再利用は custom hook（`useXxx`）に切り出す。コンポーネントに直接書かない。

### ドキュメントの書き方

- **重要なことは必ずどれかのドキュメントに書く。** 対象は「決定事項」「その理由」「守るべき制約」。
- **コードを読めばわかることは書かない。** 関数名の一覧やディレクトリ構成の羅列は不要。
- **決定が変わったら、古い記述を消して書き換える。** 追記だけして古い記述を残すと、どちらが正か分からなくなる。**消した変更は `docs/decisions.md` に1行残す。**
- **「なぜそうしたか」を必ず残す。** 理由のない決定は、後から覆すべきかどうか判断できない。
- **同じことを2か所に書かない。** 片方だけ直されて食い違う。他のドキュメントの内容が要るときはリンクする。

### ファイルが長くなったら

- **`docs/` の各ファイルが 400行を超えたら分割する。** 分割したら、このファイルの読み分け表に必ず追加する。表に無いファイルは読まれない。
- **CLAUDE.md はこれ以上増やさない。** 目次と開発ルールだけを置く場所で、仕様を書き足す場所ではない。

---

## UI / デザイン

**UI・デザインに関する作業（画面レイアウト、コンポーネント、スタイル、配色、レスポンシブ対応など）は必ず [`DESIGN.md`](DESIGN.md) を参照すること。** DESIGN.md を唯一の情報源（single source of truth）とし、画面を実装・変更する際はそこに従う。

- DESIGN.md は `shift_manage_app/DESIGN.md` と**同一内容**。片方を変えたらもう片方も同じ値に揃えること。
- DESIGN.md はマーケティングサイト由来のため、**アプリシェル（サイドバー / トップバー / ドロワー / テーブル / バッジ / チップ）を規定していない**。それらの実装例は `../shift_manage_app/mock/index.html` と、本リポジトリの `mock/index.html` にある。アプリ画面を作るときはこの mock の構造を踏襲すること。
- **2事業の色分けは mock と揃える**: プログラミング教室 = `{colors.signature-forest}`、イラスト教室 = `{colors.signature-coral}`。カレンダーのコマ、チップ、凡例すべてでこの対応を守る。

ロール別のナビゲーション構成は [`docs/architecture.md`](docs/architecture.md) にある。

---

## 現在の状態

| 項目 | 状態 |
|------|------|
| モック（`mock/index.html`） | **全画面ぶん実装済み。** 単一ファイルでダブルクリックで開く |
| DB スキーマ（`supabase/migrations/`） | 初版あり。**未反映の変更が残っている** → [`docs/data-model.md`](docs/data-model.md) |
| Web アプリ本体 | 未着手 |
| iOS アプリ | 未着手（別リポジトリ） |
